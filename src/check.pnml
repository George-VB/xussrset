 /// check

// version check
// only for OpenTTD >= r23080
if (ttd_platform != PLATFORM_OPENTTD || 
  openttd_version < version_openttd(1, 2, 0, 23080))
{
  error(NOTICE, REQUIRES_OPENTTD, string(STR_MIN_OPENTTD_VERSION));
}
