 /// header

grf
{
                   grfid: "Meo\BA";
                    name: string(STR_GRF_STRATINGS_NAME);
                    desc: string(STR_GRF_STRATINGS_DESC);
                     url: string(STR_GRF_URL);
                 version: REPO_REVISION;
  min_compatible_version: MIN_COMPATIBLE_REVISION;
}

enable_new_ratios = 1;

#include "../check.pnml"
#include "../cargotable.pnml"
#include "all-ratios.pnml"
