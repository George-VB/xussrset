 // da

#define IMAGEFILE  "src/diesel/te1.png"
purchase_sprites(da, 0, 0)
purchase_sprites(te1, 100, 0)

diesel1r_sprites(10, da,     32,  40)
diesel1r_sprites(10, te1_v1, 32, 241)
diesel1r_sprites(10, te1_v2, 32, 442)
diesel1r_sprites(10, te1_v3, 32, 643)
#undef IMAGEFILE

engine_RNDany_direction_template(da, 1, 1) 
long_vehicle_sp(da)

engine_diesel_1r_effect(da, 0, 1, 10)
engine_RNDany_effect_direction_template(da, 1, 1) 
engine_speed(da, 96)
engine_penalise_speed(da, 2)
general_running_cost(da, 236)

item (FEAT_TRAINS, da) {
  property {
    name: string(STR_NAME_DA);
    climates_available: get_climates_available();
    introduction_date: date(get_loc_year(1945), 1, 1);
    vehicle_life: 30;
    model_life: get_model_life(1945,1945);
    retire_early: get_retire_early(1945,1945);
    reliability_decay: 10;
    cost_factor: 24;
    sprite_id: SPRITE_ID_NEW_TRAIN; 
    misc_flags: bitmask(TRAIN_FLAG_TILT, TRAIN_FLAG_MU);
    track_type: get_normal_track_type_diesel();
    ai_special_flag: AI_FLAG_PASSENGER;
    power: 800 hpM;
    running_cost_base: RUNNING_COST_DIESEL;
    weight: 121 ton;
    engine_class: ENGINE_CLASS_DIESEL;
    tractive_effort_coefficient: get_tec(160.0, 121);
    length: 8;
    effect_spawn_model_and_powered: EFFECT_SPAWN_MODEL_DIESEL;
    cargo_allow_refit: [MAIL];
    default_cargo_type: MAIL;
    cargo_capacity: 1;
  }
  graphics {
    default: da_sprites;
    purchase: da_purchase_sprites;
    articulated_part: articulated_part_dummy2;
    can_attach_wagon: any_can_attach_wagon;
    start_stop: any_start_stop;
    additional_text: return string(STR_PURCHASE_HINT_ENGINE, 
                                   string(STR_PURCHASE_HINT_ENGINE_TYPE_PASSENGER_FREIGHT_DIESEL), 
                                   string(STR_PURCHASE_HINT_SECTIONS_1_2), 
                                   string(STR_PURCHASE_HINT_IN, 1945), 
                                   string(STR_PURCHASE_HINT_FACTORY_ALCO));
    speed: da_speed;
    purchase_speed: da_purchase_speed;
    running_cost_factor: da_running_cost_factor;
    bitmask_vehicle_info: da_bitmask_vehicle_info;
    cargo_capacity: return 0;
    create_effect: da_create_effect;
  }
}

if (enable_long_names) {
  item (FEAT_TRAINS, da) {
    property { name: string(STR_LONGNAME_DA); }
  }
}

if (models_default_cargo == 1) {
  item (FEAT_TRAINS, da) {
    property {
      cargo_allow_refit: [GOOD];
      default_cargo_type: GOOD;
    }
  }
}

if (models_default_cargo == 2) {
  item (FEAT_TRAINS, da) {
    property {
      cargo_allow_refit: [PASS];
      default_cargo_type: PASS;
    }
  }
}

if (models_default_cargo == 3) {
  item (FEAT_TRAINS, da) {
    property {
      cargo_allow_refit: [MAIL];
      default_cargo_type: MAIL;
    }
  }
}

 // te1

switch (FEAT_TRAINS, SELF, te1_sprites_left,
  cargo_subtype)
{
  0: te1_v1_sprites_left;
  1: te1_v2_sprites_left;
  2: te1_v3_sprites_left;
     align_10_sprites;
}

switch (FEAT_TRAINS, SELF, te1_sprites_right,
  cargo_subtype)
{
  0: te1_v1_sprites_right;
  1: te1_v2_sprites_right;
  2: te1_v3_sprites_right;
     align_10_sprites;
}

engine_RNDany_direction_template(te1, 1, 1) 
long_vehicle_sp(te1)

switch (FEAT_TRAINS, SELF, te1_cargo_subtype_text,
  cargo_subtype)
{
  0: return string(STR_REFIT_TTD_53);
  1: return string(STR_REFIT_TTD_54);
  2: return string(STR_REFIT_TTD_93);
     return CB_RESULT_NO_TEXT;
}

engine_diesel_1r_effect(te1, 0, 1, 10)
engine_RNDany_effect_direction_template(te1, 1, 1) 
engine_speed_change_after(te1, 93, 95, 1956)
engine_penalise_speed(te1, 2)
general_running_cost(te1, 198)

item (FEAT_TRAINS, te1) {
  property {
    name: string(STR_NAME_TE1);
    climates_available: get_climates_available();
    introduction_date: date(get_loc_year(1947), 1, 1);
    vehicle_life: 35;
    model_life: get_model_life(1947,1950);
    retire_early: get_retire_early(1947,1950);
    reliability_decay: 10;
    cargo_allow_refit: [MAIL];
    default_cargo_type: MAIL;
    cost_factor: 20;
    sprite_id: SPRITE_ID_NEW_TRAIN; 
    misc_flags: bitmask(TRAIN_FLAG_TILT, TRAIN_FLAG_MU);
    refit_cost: 0;
    track_type: get_normal_track_type_diesel();
    ai_special_flag: AI_FLAG_PASSENGER;
    power: 710 hpM;
    running_cost_base: RUNNING_COST_DIESEL;
    cargo_capacity: 1;
    weight: 124 ton;
    engine_class: ENGINE_CLASS_DIESEL;
    tractive_effort_coefficient: get_tec(157.0, 124);
    length: 8;
    visual_effect_and_powered: diesel_visual_effect_and_powered(0);
  }
  graphics {
    default: te1_sprites;
    purchase: te1_purchase_sprites;
    articulated_part: articulated_part_dummy2;
    can_attach_wagon: any_can_attach_wagon;
    start_stop: any_start_stop;
    additional_text: return string(STR_PURCHASE_HINT_ENGINE, 
                                   string(STR_PURCHASE_HINT_ENGINE_TYPE_PASSENGER_FREIGHT_DIESEL), 
                                   string(STR_PURCHASE_HINT_SECTIONS_1_2), 
                                   string(STR_PURCHASE_HINT_FROMTO, 1947, 1950), 
                                   string(STR_PURCHASE_HINT_FACTORY_KHARKIV));
    cargo_subtype_text: te1_cargo_subtype_text;
    cargo_capacity: return 0;
    speed: te1_speed;
    purchase_speed: te1_purchase_speed;
    running_cost_factor: te1_running_cost_factor;
    bitmask_vehicle_info: te1_bitmask_vehicle_info;
    create_effect: te1_create_effect;
  }
}

if (enable_long_names) {
  item (FEAT_TRAINS, te1) {
    property { name: string(STR_LONGNAME_TE1); }
  }
}

if (models_default_cargo == 1) {
  item (FEAT_TRAINS, te1) {
    property {
      cargo_allow_refit: [GOOD];
      default_cargo_type: GOOD;
    }
  }
}

if (models_default_cargo == 2) {
  item (FEAT_TRAINS, te1) {
    property {
      cargo_allow_refit: [PASS];
      default_cargo_type: PASS;
    }
  }
}

if (models_default_cargo == 3) {
  item (FEAT_TRAINS, te1) {
    property {
      cargo_allow_refit: [MAIL];
      default_cargo_type: MAIL;
    }
  }
}

