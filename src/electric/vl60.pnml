 // n60

// ВЛ60 - первый массовый отечественный электровоз переменного тока.
// Н60 - предсерийная версия

#define IMAGEFILE  "src/electric/vl60.png"
purchase_sprites(n60, 0, 0)
purchase_sprites(vl60_v1, 100, 0)

electric1_2_sprites(12, n60,     32,   40)
electric1_2_sprites(12, vl60_v1, 32,  241)
electric1_2_sprites(12, vl60_v2, 32,  442)
#undef IMAGEFILE

switch (FEAT_TRAINS, SELF, vl60_purchase_sprites,
  current_year >= 1960)
{
  0: n60_purchase_sprites;
     vl60_v1_purchase_sprites;
}

long_vehicle(n60)

engine_electric1_2_effect(n60, -2, 6)
engine_speed(n60, 100)
engine_penalise_speed0(n60)
engine_RC_change_build_after(vl60, 485, 
                                   451, 1960)

item (FEAT_TRAINS, n60) {
  property {
    name: string(STR_NAME_N60);
    climates_available: NO_CLIMATE;
    introduction_date: date(get_loc_year(1959), 1, 1);
    vehicle_life: 20;
    model_life: get_model_life(1959,1959);
    retire_early: get_retire_early(1959,1959);
    reliability_decay: 10;
    cost_factor: 76;
    sprite_id: SPRITE_ID_NEW_TRAIN; 
    misc_flags: bitmask(TRAIN_FLAG_TILT, TRAIN_FLAG_MU);
    track_type: get_ac_track_type();
    ai_special_flag: AI_FLAG_PASSENGER;
    power: 4470 hpM;
    running_cost_base: RUNNING_COST_ELECTRIC;
    weight: 140 ton;
    engine_class: ENGINE_CLASS_ELECTRIC;
    tractive_effort_coefficient: get_tec(255.0, 140);
    length: 8;
    cargo_allow_refit: [MAIL];
    default_cargo_type: MAIL;
    cargo_capacity: 1;
    effect_spawn_model_and_powered: EFFECT_SPAWN_MODEL_ELECTRIC;
  }
  graphics {
    default: n60_sprites;
    purchase: n60_purchase_sprites;
    articulated_part: articulated_part_dummy4;
    can_attach_wagon: any_can_attach_wagon;
    start_stop: any_start_stop;
    additional_text: return string(STR_PURCHASE_HINT_ENGINE, 
                                   string(STR_PURCHASE_HINT_ENGINE_TYPE_PASSENGER_FREIGHT_ELECTRIC_AC_PROTOTYPE), 
                                   string(STR_PURCHASE_HINT_SECTIONS_1), 
                                   string(STR_PURCHASE_HINT_IN, 1959), 
                                   string(STR_PURCHASE_HINT_FACTORY_NOVOCHERKASSK));
    speed: n60_speed;
    purchase_speed: n60_purchase_speed;
    bitmask_vehicle_info: n60_bitmask_vehicle_info;
    cargo_capacity: return 0;
    create_effect: n60_create_effect;
    running_cost_factor: vl60_v1_running_cost_factor;
  }
}

if (enable_long_names) {
  item (FEAT_TRAINS, n60) {
    property { name: string(STR_LONGNAME_N60); }
  }
}

if (models_default_cargo == 1) {
  item (FEAT_TRAINS, n60) {
    property {
      cargo_allow_refit: [GOOD];
      default_cargo_type: GOOD;
    }
  }
}

if (models_default_cargo == 2) {
  item (FEAT_TRAINS, n60) {
    property {
      cargo_allow_refit: [PASS];
      default_cargo_type: PASS;
    }
  }
}

if (models_default_cargo == 3) {
  item (FEAT_TRAINS, n60) {
    property {
      cargo_allow_refit: [MAIL];
      default_cargo_type: MAIL;
    }
  }
}

 // vl60

switch (FEAT_TRAINS, SELF, vl60_sprites_start,
  ((build_year >= 1960) * 2) +
  (LOAD_TEMP(2) >= date(1972, 1, 1)))
{
  0..1: n60_sprites_start;
  2: vl60_v1_sprites_start;
  3: vl60_v2_sprites_start;
     align_12_sprites; 
}

long_vehicle(vl60)

switch (FEAT_TRAINS, SELF, vl60_cargo_subtype_text,
  build_year >= 1960)
{
  0: return string(STR_REFIT_FACTORY_GENERAL_TYPE, string(STR_NAME_N60), "");
     return CB_RESULT_NO_TEXT;
}

switch (FEAT_TRAINS, SELF, vl60_additional_text,
  current_year >= 1960)
{
  0: return string(STR_PURCHASE_HINT_ENGINE, 
                   string(STR_PURCHASE_HINT_ENGINE_TYPE_PASSENGER_FREIGHT_ELECTRIC_AC_PROTOTYPE), 
                   string(STR_PURCHASE_HINT_SECTIONS_1), 
                   string(STR_PURCHASE_HINT_IN, 1959), 
                   string(STR_PURCHASE_HINT_FACTORY_NOVOCHERKASSK));
     return string(STR_PURCHASE_HINT_ENGINE, 
                   string(STR_PURCHASE_HINT_ENGINE_TYPE_PASSENGER_FREIGHT_ELECTRIC_AC), 
                   string(STR_PURCHASE_HINT_SECTIONS_1_2_WITH_ATTACH, string(STR_PURCHASE_HINT_LIST2, string(STR_NAME_VL60K), string(STR_NAME_VL60PK))), 
                   string(STR_PURCHASE_HINT_FROMTO, 1960, 1965), 
                   string(STR_PURCHASE_HINT_FACTORY_NOVOCHERKASSK));
}

engine_electric1_2_effect(vl60, -2, 6)
engine_speed(vl60, 100)
engine_penalise_speed3(vl60, vl60k, vl60pk, 2)
engine_TE_change_build_after(vl60, get_tec_int(255.0, 140), 
                                   get_tec_int(228.0, 138), 1960)
engine_power_change_build_after(vl60, get_power_in_cb(4470), 
                                      get_power_in_cb(4380), 1960)
engine_weight_change_build_after(vl60, 140, 
                                       138, 1960)
engine_cost_change_build_after(vl60, 76, 
                                     64, 1960)

switch (FEAT_TRAINS, SELF, vl60_bitmask_vehicle_info_advanced2,
  (position_in_consist == 0) || vehicle_is_not_powered)
{ 1: return 0;                                                              
     return FLAG_PENALISE_SPEED; }

switch (FEAT_TRAINS, PARENT, vl60_bitmask_vehicle_info_advanced1,
  build_year >= 1960)
{
  0: return FLAG_PENALISE_SPEED;
     vl60_bitmask_vehicle_info;
}    
                  
switch (FEAT_TRAINS, SELF, vl60_bitmask_vehicle_info_advanced,
  build_year >= 1960)
{
  0: vl60_bitmask_vehicle_info_advanced2;
     vl60_bitmask_vehicle_info_advanced1;
}    

item (FEAT_TRAINS, vl60) {
  property {
    name: string(STR_NAME_VL60);
    climates_available: get_climates_available();
    introduction_date: date(get_loc_year(1957), 1, 1);
    vehicle_life: 30;
    model_life: get_model_life(1957,1965);
    retire_early: get_retire_early(1957,1965);
    reliability_decay: 10;
    cost_factor: 64;
    sprite_id: SPRITE_ID_NEW_TRAIN; 
    misc_flags: bitmask(TRAIN_FLAG_TILT, TRAIN_FLAG_MU);
    track_type: get_ac_track_type();
    ai_special_flag: AI_FLAG_PASSENGER;
    power: 4380 hpM;
    running_cost_base: RUNNING_COST_ELECTRIC;
    weight: 138 ton;
    engine_class: ENGINE_CLASS_ELECTRIC;
    tractive_effort_coefficient: get_tec(228.0, 138);
    length: 8;
    cargo_allow_refit: [MAIL];
    default_cargo_type: MAIL;
    cargo_capacity: 1;
    effect_spawn_model_and_powered: EFFECT_SPAWN_MODEL_ELECTRIC;
  }
  graphics {
    default: vl60_sprites;
    purchase: vl60_purchase_sprites;
    cost_factor: vl60_cost_factor;
    articulated_part: articulated_part_dummy4;
    can_attach_wagon: any_can_attach_wagon;
    start_stop: any_start_stop;
    additional_text: vl60_additional_text;
    speed: vl60_speed;
    power: vl60_power;                           
    tractive_effort_coefficient: vl60_tractive_effort_coefficient;     
    weight: vl60_weight;                          
    purchase_speed: vl60_purchase_speed;
    bitmask_vehicle_info: vl60_bitmask_vehicle_info_advanced;
    cargo_capacity: return 0;
    cargo_subtype_text: vl60_cargo_subtype_text;
    create_effect: vl60_create_effect;
    running_cost_factor: vl60_running_cost_factor;
  }
}

if (enable_long_names) {
  item (FEAT_TRAINS, vl60) {
    property { name: string(STR_LONGNAME_VL60); }
  }
}

if (models_default_cargo == 1) {
  item (FEAT_TRAINS, vl60) {
    property {
      cargo_allow_refit: [GOOD];
      default_cargo_type: GOOD;
    }
  }
}

if (models_default_cargo == 2) {
  item (FEAT_TRAINS, vl60) {
    property {
      cargo_allow_refit: [PASS];
      default_cargo_type: PASS;
    }
  }
}

if (models_default_cargo == 3) {
  item (FEAT_TRAINS, vl60) {
    property {
      cargo_allow_refit: [MAIL];
      default_cargo_type: MAIL;
    }
  }
}

