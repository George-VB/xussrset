 // vl80s

// ВЛ80с "Выло" (1979-1995-2003)

#define IMAGEFILE  "src/electric/vl80s.png"
purchase_sprites(vl80s, 0, 0)

electric2_1_sprites(9, vl80s, 32,  40)
#undef IMAGEFILE

engine_direction_template(vl80s, ) 
long_vehicle(vl80s)

engine_electric2_1_effect(vl80s, -2, 4)
engine_direction_template(vl80s, _create_effect) 
engine_speed_with_reduce(vl80s, 110, 100, 30)
engine_penalise_speed(vl80s, 4)
general_running_cost(vl80s, 372)

item (FEAT_TRAINS, vl80s) {
  property {
    name: string(STR_VL80S_NAME);
    climates_available: get_climates_available();
    introduction_date: date(get_loc_year(1979), 1, 1);
    vehicle_life: 30;
    model_life: get_model_life(1979,2003);
    retire_early: get_retire_early(1979,2003);
    reliability_decay: 10;
    cargo_allow_refit: [GOOD];
    default_cargo_type: GOOD;
    cost_factor: 53;
    sprite_id: SPRITE_ID_NEW_TRAIN; 
    speed: 110 km/h;
    misc_flags: bitmask(TRAIN_FLAG_TILT);
    refit_cost: 0;
    track_type: get_ac_track_type();
    ai_special_flag: AI_FLAG_PASSENGER;
    power: 4055 hpM;
    running_cost_base: RUNNING_COST_ELECTRIC;
    cargo_capacity: 1;
    weight: 92 ton;
    engine_class: ENGINE_CLASS_ELECTRIC;
    tractive_effort_coefficient: get_tec(201.0, 92);
    length: 8;
    effect_spawn_model_and_powered: EFFECT_SPAWN_MODEL_ELECTRIC;
  }
  graphics {
    default: vl80s_sprites;
    purchase: vl80s_purchase_sprites;
    articulated_part: articulated_part_dummy1;
    can_attach_wagon: any_can_attach_wagon;
    start_stop: any_start_stop;
    additional_text: return string(STR_PURCHASE_HINT_ENGINE, 
                                   string(STR_PURCHASE_HINT_ENGINE_TYPE_FREIGHT_ELECTRIC_AC), 
                                   string(STR_PURCHASE_HINT_SECTIONS_2_4), 
                                   string(STR_PURCHASE_HINT_FROMTO, 1979, 2003), 
                                   string(STR_PURCHASE_HINT_FACTORY_NOVOCHERKASSK_ELP));
    cargo_capacity: return 0;
    speed: vl80s_speed;
    purchase_speed: vl80s_purchase_speed;
    bitmask_vehicle_info: vl80s_bitmask_vehicle_info;
    create_effect: vl80s_create_effect_sprites_start;
    running_cost_factor: vl80s_running_cost_factor;
  }
}

if (enable_long_names) {
  item (FEAT_TRAINS, vl80s) {
    property { name: string(STR_VL80S_NAME_LONG); }
  }
}

