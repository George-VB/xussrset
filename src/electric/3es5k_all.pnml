 // 3es5k_all
// 467

item (FEAT_TRAINS, _3es5k_all, 467) {
  property { 
    name: string(STR_NAME_2ES5K);
    vehicle_no_dates(2004, 2050, 30, 10)
    vehicle_cargo()
    vehicle_acelectric(4072, 96, 363.0)
    cost_factor: 55;
    length: 8;
  }
  graphics {
    default: _3es5k_sprites;
    purchase: _3es5k_purchase_sprites;
    articulated_part: articulated_part_dummy2;
    can_attach_wagon: any_can_attach_wagon;
    start_stop: any_start_stop;
    additional_text: return string(STR_PURCHASE_HINT_ENGINE, 
                                 string(STR_PURCHASE_HINT_ENGINE_TYPE_FREIGHT_ELECTRIC_AC), 
                                 string(STR_PURCHASE_HINT_SECTIONS_2_4), 
                                 string(STR_PURCHASE_HINT_FROM, 2004), 
                                 string(STR_PURCHASE_HINT_FACTORY_NOVOCHERKASSK));
    speed: _3es5k_speed;
    purchase_speed: _3es5k_purchase_speed;
    bitmask_vehicle_info: _3es5k_bitmask_vehicle_info;
    cargo_capacity: return 0;
    create_effect: _3es5k_create_effect_sprites_start;
    running_cost_factor: _3es5k_running_cost_factor;
    colour_mapping: return ttdall_cc + 0xC9;
  }
}

long_name_template(_3es5k_all, STR_LONGNAME_2ES5K)
models_default_cargo_template(_3es5k_all)

if (models_details_level == 0) {
  item (FEAT_TRAINS, _2es5k) {
    property { climates_available: NO_CLIMATE; }
  }
  item (FEAT_TRAINS, _3es5k) {
    property { climates_available: NO_CLIMATE; }
  }
  item (FEAT_TRAINS, _3es5k_all) {
    property { climates_available: get_climates_available(); }
  }
}

allow_acelectric(_3es5k_all)
