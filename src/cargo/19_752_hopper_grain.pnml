 // 19_752_hopper_grain

/// Вагон
// 19-752 хоппер-зерновоз (1982-1989)
// hopper grain

#define IMAGEFILE  "src/cargo/19-752-hopper-grain-1982-1989.png"

// покупка
spriteset (_19_752_hopper_grain_purchase_spriteset, IMAGEFILE)
{
  s9_purchase_template(0, 18, 0)
}

// вид 1
spriteset (_19_752_hopper_grain_spriteset, IMAGEFILE)
{
  s9_template(0, 18, 0)
}

#undef IMAGEFILE

// Если рисуем на карте и следующий в цепочке вагон скрыт, то
// отдаём пустышку
switch (FEAT_TRAINS, SELF, _19_752_hopper_grain_get_spriteset,
  is_drawn_in_viewport() && is_hidden_at(1))
{
  1: dummy_spriteset;
  _19_752_hopper_grain_spriteset;
}

item (FEAT_TRAINS, _19_752_hopper_grain) 
{
  property
  {
    name: string(STR_19_752_HOPPER_GRAIN_NAME);
    climates_available: bitmask(CLIMATE_TEMPERATE, CLIMATE_ARCTIC, CLIMATE_TROPICAL);
    introduction_date: date(get_car_year(1982), 1, 1);
    model_life: 26;
    vehicle_life: 26;
    reliability_decay: 20;
    refittable_cargo_classes: bitmask(CC_BULK); // Разрешить
    non_refittable_cargo_classes: NO_CARGO_CLASS; // Запретить другие
    refittable_cargo_types: 0; // bitmask(GOOD, VALU);
    refit_cost: 0;
    cost_factor: 2;
    running_cost_factor: 10;
    sprite_id: SPRITE_ID_NEW_TRAIN;
    speed: 120 km/h;
    misc_flags: bitmask(TRAIN_FLAG_MU);
    track_type: RAIL;
    power: 0 hpM;
    running_cost_base: RUNNING_COST_DIESEL;
    loading_speed: 18;
    cargo_capacity: 70;
    weight: 23 ton;
    shorten_vehicle: SHORTEN_TO_8_8;
    visual_effect_and_powered: 
      visual_effect_and_powered(VISUAL_EFFECT_DISABLE, 0, DISABLE_WAGON_POWER);
    bitmask_vehicle_info: 0;
  }
  graphics
  {
    default: _19_752_hopper_grain_get_spriteset;
    purchase: _19_752_hopper_grain_purchase_spriteset;
    articulated_part: articulated_part_dummy1;
    additional_text: return string(STR_19_752_HOPPER_GRAIN_PURCHASE_HINT);
  }
}
