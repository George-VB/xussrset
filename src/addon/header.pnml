 /// header

grf
{
                   grfid: "Meo\B0";
                    name: string(STR_GRF_ADDON_NAME);
                    desc: string(STR_GRF_ADDON_DESC);
                     url: string(STR_GRF_URL);
                 version: REPO_REVISION;
  min_compatible_version: MIN_COMPATIBLE_REVISION;
  param 6
  {
    disable_icons
    {
           name: string(STR_PARAM_DISABLE_ICONS_NAME);
           desc: string(STR_PARAM_DISABLE_ICONS_DESC);
           type: bool;
            bit: 6;
      def_value: 0;
    }
    disable_long_names
    {
           name: string(STR_PARAM_DISABLE_LONG_NAMES_NAME);
           desc: string(STR_PARAM_DISABLE_LONG_NAMES_DESC);
           type: bool;
            bit: 7;
      def_value: 0;
    }
    disable_rails
    {
           name: string(STR_PARAM_DISABLE_RAILS_NAME);
           desc: string(STR_PARAM_DISABLE_RAILS_DESC);
           type: bool;
            bit: 5;
      def_value: 0;
    }
  }
  param 1
  {
    speed_penalty_percent
    {
           type: int;
           name: string(STR_PARAM_SPEED_PENALTY_PERCENT_NAME);
           desc: string(STR_PARAM_SPEED_PENALTY_PERCENT_DESC);
      min_value: 10;
      max_value: 50;
      def_value: 15;
    }
  }
  param 3
  {
    costs_multiplier
    {
           type: int;
           name: string(STR_PARAM_CBASE_NAME);
           desc: string(STR_PARAM_CBASE_DESC);
      min_value: 1;
      max_value: 10000;
      def_value: 100;
    }
  }
  param 4
  {
    rcosts_multiplier
    {
           type: int;
           name: string(STR_PARAM_RCBASE_NAME);
           desc: string(STR_PARAM_RCBASE_DESC);
      min_value: 1;
      max_value: 10000;
      def_value: 100;
    }
  }
  param 5
  {
    new_cargo_ageing
    {
           type: int;
           name: string(STR_PARAM_CA_BASE_NAME);
           desc: string(STR_PARAM_CA_BASE_DESC);
      min_value: 1;
      max_value: 10000;
      def_value: 100;
    }
  }
}
