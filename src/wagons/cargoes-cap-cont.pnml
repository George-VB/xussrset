// CAP

switch (FEAT_TRAINS, SELF, container_cargo_age_period2, cargo_type_in_veh)
{
  cargo_check_ref_containerlist(return CAP_008_VERY_SLOW)
  return get_CAP_in_cb(CAP_001_NORMAL);
}

switch (FEAT_TRAINS, SELF, container_cargo_age_period,
  date_of_last_service >= date(1988,1,1))
{
  1: container_cargo_age_period2;
  return get_CAP_in_cb(CAP_001_NORMAL);
}
