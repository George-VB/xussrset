 // s_s

#define IMAGEFILE  "src/steam/s.png"
purchase_sprites(s_s, 0, 0)
steam_sprites(7, s_s_engine, 32,  40)
tender_sprites(5, s_s_tender, 447,  40)
#undef IMAGEFILE

steamer_direction_template(s_s, _sprites_start)
long_vehicle(s_s)

switch (FEAT_TRAINS, SELF, s_s_running_cost_factor,
[  STORE_TEMP(157, 0),       // Моторы
   STORE_TEMP(22, 1),        // Бригада
   STORE_TEMP(55, 2),        // Износ
   STORE_TEMP(0, 3),         // Сопровождение
   STORE_TEMP(39, 4),        // ТО
   STORE_TEMP(3, 5),         // Сертификация

   STORE_TEMP(115, 6),       // Скорость
   STORE_TEMP(76+64, 7),     // Тара
   STORE_TEMP(76+64, 8)])    // Максимальная масса
{ all_running_cost_factor; } // 276

engine_steam_1_effect(s_s, -3, 11)
engine_speed_change_after(s_s, 110,
                               115, 1926)
engine_penalise_speed0(s_s)
engine_tender(s_s)
engine_tender_length(s_s, 6, 1, 4, 1)
hint_engine_3fact_changeable_factory_name(s_s,
            STR_PURCHASE_HINT_ENGINE_TYPE_PASSENGER_STEAM,
            STR_PURCHASE_HINT_SECTIONS_1,
            1910, 1919,
            string(STR_PURCHASE_HINT_FACTORY_SORMOVO),
            string(STR_PURCHASE_HINT_FACTORY_NEVSKY),
            fact_kharkiv())

item (FEAT_TRAINS, s_s, 433) {
  property {
    name: string(STR_NAME_S_S);
    vehicle_dates(1910, 1919, 45, 10, 6, 12)
    vehicle_pass()
    vehicle_steam(1292, 76+64, 104.0)
  }
  graphics {
    default: s_s_sprites;
    purchase: s_s_purchase_sprites;
    articulated_part: s_s_articulated_part;
    length: s_s_length;
    can_attach_wagon: any_can_attach_wagon;
    start_stop: any_start_stop;
    speed: s_s_speed;
    purchase_speed: s_s_purchase_speed;
    bitmask_vehicle_info: s_s_bitmask_vehicle_info;
    cargo_capacity: return 0;
    create_effect: s_s_create_effect;
    running_cost_factor: s_s_running_cost_factor;
    colour_mapping: steam_pass_extra_colour_mapping;
    cargo_subtype_text: steam_pass_extra_cargo_subtype_text;
    additional_text: s_s_additional_text;
  }
}

long_name_template(s_s, STR_LONGNAME_S_S)
models_default_cargo_template(s_s)
allow_steamer(s_s)
