 // em

#define IMAGEFILE  "src/steam/em.png"
purchase_sprites(e_em, 0, 0)
steam_sprites(7, e_em_engine, 32, 40)
#undef IMAGEFILE

switch (FEAT_TRAINS, SELF, e_em_tender_sprites_start, 0)
{
  e_e_tender_sprites_start;
}

steamer_direction_template(e_em, _sprites_start)
long_vehicle(e_em)

switch (FEAT_TRAINS, SELF, e_em_b1933_running_cost_factor,
[  STORE_TEMP(163, 0),       // Моторы
   STORE_TEMP(23, 1),        // Бригада
   STORE_TEMP(57, 2),        // Износ
   STORE_TEMP(0, 3),         // Сопровождение
   STORE_TEMP(40, 4),        // ТО
   STORE_TEMP(3, 5),         // Сертификация

   STORE_TEMP(55 + 10 * (current_year >= 1936), 6),        // Скорость
   STORE_TEMP(153, 7),     		/* 85+68 */	// Тара
   STORE_TEMP(153, 8)])    		/* 85+68 */	// Максимальная масса
{ all_running_cost_factor; } // 286

switch (FEAT_TRAINS, SELF, e_em_a1933_running_cost_factor,
[  STORE_TEMP(153, 0),       // Моторы
   STORE_TEMP(22, 1),        // Бригада
   STORE_TEMP(54, 2),        // Износ
   STORE_TEMP(0, 3),         // Сопровождение
   STORE_TEMP(38, 4),        // ТО
   STORE_TEMP(3, 5),         // Сертификация

   STORE_TEMP(55 + 10 * (current_year >= 1936), 6),        // Скорость
   STORE_TEMP(146, 7),     		/* 78+68 */	// Тара
   STORE_TEMP(146, 8)])    		/* 78+68 */	// Максимальная масса
{ all_running_cost_factor; } // 269

switch (FEAT_TRAINS, SELF, e_em_a1934_running_cost_factor,
[  STORE_TEMP(162, 0),       // Моторы
   STORE_TEMP(23, 1),        // Бригада
   STORE_TEMP(57, 2),        // Износ
   STORE_TEMP(0, 3),         // Сопровождение
   STORE_TEMP(40, 4),        // ТО
   STORE_TEMP(3, 5),         // Сертификация

   STORE_TEMP(55 + 10 * (current_year >= 1936), 6),        // Скорость
   STORE_TEMP(157, 7),     		/* 89+68 */	// Тара
   STORE_TEMP(157, 8)])    		/* 89+68 */	// Максимальная масса
{ all_running_cost_factor; } // 285

RC_build_year2_template(e_em, 1933, 1934)
engine_tender(e_em)
engine_tender_length(e_em, 6, 1, 4, 1)
engine_weight_change_build_after2(e_em, 85+68,
                                        78+68, 1933,
                                        89+68, 1934)
engine_TE_change_build_after2(e_em, get_tec_int(218.0, 85+68),
                                    get_tec_int(218.0, 78+68), 1933,
                                    get_tec_int(218.0, 89+68), 1934)
engine_speed_change_after(e_em, 55,
                                65, 1936)
engine_penalise_speed0(e_em)
hint_engine(e_em,
     STR_PURCHASE_HINT_ENGINE_TYPE_FREIGHT_STEAM,
     STR_PURCHASE_HINT_SECTIONS_1,
     1931, 1935,
     fact_lugansk())

item (FEAT_TRAINS, e_em, 332) {
  property {
    name: string(STR_NAME_EM);
    vehicle_dates(1931, 1935, 30, 10, 6, 12)
    vehicle_cargo()
    vehicle_steam(1460, 85+68, 218.0)
  }
  graphics {
    default: e_em_sprites;
    purchase: e_em_purchase_sprites;
    articulated_part: e_em_articulated_part;
    length: e_em_length;
    can_attach_wagon: any_can_attach_wagon;
    start_stop: any_start_stop;
    speed: e_em_speed;
    purchase_speed: e_em_purchase_speed;
    bitmask_vehicle_info: e_em_bitmask_vehicle_info;
    cargo_capacity: return 0;
    tractive_effort_coefficient: e_em_tractive_effort_coefficient;
    weight: e_em_weight;
    create_effect: e_create_effect;
    running_cost_factor: e_em_running_cost_factor;
    colour_mapping: any_cc_colour;
    additional_text: e_em_additional_text;
  }
}

long_name_template(e_em, STR_LONGNAME_EM)
models_default_cargo_template(e_em)
allow_steamer(e_em)
