 // te

#define IMAGEFILE  "src/steam/te.png"
purchase_sprites(te, 0, 0)
steam_sprites(8, te_engine, 32,  40)
tender_sprites(5, te_tender, 447,  40)
#undef IMAGEFILE

steamer_direction_template(te, _sprites_start)
long_vehicle(te)

engine_tender(te)
engine_tender_length(te, 7, 1, 4, 1)

switch (FEAT_TRAINS, SELF, te_running_cost_factor,
[  STORE_TEMP(215, 0),       // Моторы
   STORE_TEMP(30, 1),        // Бригада
   STORE_TEMP(76, 2),        // Износ
   STORE_TEMP(0, 3),         // Сопровождение
   STORE_TEMP(53, 4),        // ТО
   STORE_TEMP(4, 5),         // Сертификация

   STORE_TEMP(80, 6),         // Скорость
   STORE_TEMP(86+65, 7),      // Тара
   STORE_TEMP(86+65, 8)])     // Максимальная масса
{ all_running_cost_factor; }  // 378

engine_speed(te, 80)
engine_steam_1_effect(te, -3, 13)
engine_penalise_speed0(te)
hint_engine_6fact(te,
     STR_PURCHASE_HINT_ENGINE_TYPE_FREIGHT_STEAM,
     STR_PURCHASE_HINT_SECTIONS_1,
     1943, 1947,
     STR_PURCHASE_HINT_FACTORY_LOFAG,
     STR_PURCHASE_HINT_FACTORY_HENSCHEL,
     STR_PURCHASE_HINT_FACTORY_SCHWARTZKOPFF,
     STR_PURCHASE_HINT_FACTORY_KRAUSS_MAFFEI,
     STR_PURCHASE_HINT_FACTORY_RHEINMETAL_BORSIG,
     STR_PURCHASE_HINT_FACTORY_SCHICHAU)

item (FEAT_TRAINS, te, 512) {
  property {
    name: string(STR_NAME_TE);
    vehicle_dates(1943, 1947, 30, 10, 7, 16)
    vehicle_cargo()
    vehicle_steam(1516, 86+65, 208.0)
  }
  graphics {
    default: te_sprites;
    purchase: te_purchase_sprites;
    articulated_part: te_articulated_part;
    length: te_length;
    can_attach_wagon: any_can_attach_wagon;
    start_stop: any_start_stop;
    speed: te_speed;
    purchase_speed: return 80;
    bitmask_vehicle_info: te_bitmask_vehicle_info;
    cargo_capacity: return 0;
    create_effect: te_create_effect;
    running_cost_factor: te_running_cost_factor;
    colour_mapping: steam_freight_colour_mapping;
    cargo_subtype_text: steam_freight_cargo_subtype_text;
    additional_text: te_additional_text;
  }
}

long_name_template(te, STR_LONGNAME_TE)
models_default_cargo_template(te)
allow_steamer(te)
